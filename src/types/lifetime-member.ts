/**
 * Type definition for Lifetime Member
 * Strict typing without 'any' types
 */

export interface LifetimeMember {
  readonly id: number;
  readonly name: string;
  readonly memberNumber: string;
  readonly dateOfBirth: string;
  readonly image: string;
}
